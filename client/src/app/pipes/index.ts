import { Type } from '@angular/core';
import { SearchFilterPipe } from './search-filter.pipe';

export const PIPES: Array<Type<any> | any[]> = [SearchFilterPipe];

export * from './search-filter.pipe';
